function checkMediaQuery(query){var all=$('#screenSize').css('font-family').replace(/['"]+/g,'').split(', ');return(all.indexOf(query)>-1);}
function updateOnResize(){dlog('Resize updates executed...');ux.viewport.width=$(window).width();ux.viewport.height=$(window).height();}
function updateOnScroll(){dlog('Scroll updates executed...');var treshold=5;ux.scroll.offsetPrevious=ux.scroll.offset;ux.scroll.offset=$(window).scrollTop();if(ux.scroll.offset-ux.scroll.offsetPrevious>treshold){ux.scroll.direction='down';}else if(ux.scroll.offsetPrevious-ux.scroll.offset>treshold){ux.scroll.direction='up';}
ux.viewport.visibleTop=ux.scroll.offset
ux.viewport.visibleBottom=ux.viewport.height+ux.scroll.offset;}
function lightScroll(callback,delay){var timer,delay=delay||80,scrollCounter=0;$(window).on('scroll',function(e){scrollCounter++;if(scrollCounter>8){dlog_verbose('Scroll tick');callback();scrollCounter=0;timer=false;return false;}
clearTimeout(timer);timer=setTimeout(function(){scrollCounter=0;dlog_verbose('Scroll tick');callback();},delay);});};function lightResize(callback,delay){var timer,delay=delay||100;$(window).on('resize',function(e){clearTimeout(timer);timer=setTimeout(function(){dlog_verbose('Resize tick');callback();},delay);});};var loadCSS=function(href){var cssLink=$("<link rel='stylesheet' type='text/css' href='"+href+"'>");$('head').append(cssLink);};queue.jQuery(function(){(function($){$.fn.animateAuto=function(){this.each(function(i,el){var clone=$(el).clone().css({'height':'auto','width':$(el).width(),'position':'absolute','left':'-3000px'}).appendTo("body");var height=clone.height();clone.remove();$(el).css({'height':height});});return this;}})(window.jQuery||window.Zepto||window.$);updateOnResize();updateOnScroll();});